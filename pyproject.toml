[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Manual de Integração do PNCP"
authors = [{name = "PNCP", email = "pncp@gestao.gov.br"}]
dynamic = ["version", "description"]